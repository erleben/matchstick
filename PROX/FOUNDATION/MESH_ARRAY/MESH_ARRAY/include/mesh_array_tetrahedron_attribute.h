#ifndef MESH_ARRAY_TETRAHEDRON_ATTRIBUTE_H
#define MESH_ARRAY_TETRAHEDRON_ATTRIBUTE_H

namespace mesh_array 
{
  
  /**
   * Tetrahedron Attribute Class.
   *
   * @tparam T    The attribute type to be stored for each vertex
   * @tparam M    The mesh type of the vertices.
   */
  template<typename T, typename M> 
  class TetrahedronAttribute 
  {
  };
  
} // end namespace mesh_array 

// MESH_ARRAY_TETRAHERON_ATTRIBUTE_H
#endif 
